# {{ pipeline_name }}

> Generated by [ragfactory](https://github.com/ragfactory/ragfactory).
> Edit the generated code freely — it belongs to you.

A production-ready RAG pipeline built with **{{ framework }}**.

## Quick Start

### 1. Install dependencies

```bash
pip install .
```

### 2. Configure secrets

```bash
cp .env.example .env
# Fill in your API keys in .env
```

### 3. Index your documents

```bash
python ingestion.py path/to/documents
```

### 4. Query the pipeline

```python
from pipeline import run_pipeline

answer = run_pipeline("Your question here")
print(answer)
```

## Dependencies

{% for dep in dependencies %}
- `{{ dep }}`
{% endfor %}
{% if env_vars %}

## Environment Variables

| Variable | Description |
|----------|-------------|
{% for var_name, description in env_vars %}
| `{{ var_name }}` | {{ description }} |
{% endfor %}

{% endif %}

## Regenerate

If you update the ragfactory config, regenerate the pipeline:

```bash
ragfactory generate config.yaml --out ./
```

## License

This generated code is released under [Apache-2.0](LICENSE).
