# stub: langchain pipeline entrypoint
FRAMEWORK = "{{ framework }}"
PIPELINE_NAME = "{{ pipeline_name }}"
PROMPT_TEMPLATE = {{ generation.prompt_template | to_py_literal }}
# stages:
{{ stages.chunking }}
{{ stages.embedding }}
{{ stages.vectordb }}
{{ stages.retrieval }}
{% if stages.reranker is defined and stages.reranker %}{{ stages.reranker }}{% endif %}
{{ stages.llm }}
